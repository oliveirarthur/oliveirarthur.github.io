# oliveirarthur.github.io

Arthur Oliveira's website

## dev server
```
docker-compose up dev
# OR
docker run --rm -v $(pwd):/srv/jekyll -v /tmp/jekyll:/usr/local/bundle -p 4000:4000 -it jekyll/jekyll jekyll serve --watch
```
